import React from 'react';
import {StyleSheet, Text, View, Button, Touchopacity} from 'react-native';
import StackPage from './stack';

const App = ({navigation}) => {
  return <StackPage />;
};

export default App;